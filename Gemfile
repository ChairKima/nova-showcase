source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins 
# This line helps ensure local build uses versions compatible with GitHub Pages
# The theme itself is specified in _config.yml
